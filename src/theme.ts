import { createMuiTheme } from '@material-ui/core'

export const theme = createMuiTheme({
  palette: {
    primary: {
      main: 'rgb(230, 53, 0)',
    },
    secondary: {
      main: 'rgb(255, 195, 22)',
    },
  },
})

export default theme
